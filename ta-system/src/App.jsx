import React from 'react';
import LoginPage from './LoginPage'; // Import the LoginPage component

const App = () => {
  return (
    <div>
      <LoginPage />
    </div>
  );
};

export default App;
